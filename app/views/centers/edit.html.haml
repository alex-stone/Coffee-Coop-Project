%h1= t(:edit_center)

= render 'form'

= link_to t(:show), @center, :class => 'btn'
= link_to t(:back), centers_path, :class => 'btn btn-edit'
- if @is_admin
  = link_to t(:destroy), center_path, :confirm => 'Are you sure?', :method => 'delete', :class => 'btn btn-delete'
