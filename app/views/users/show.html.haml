%h1 User ##{@user.id}
%p
  %b Name:
  = @user.name
%p
  %b Email:
  = @user.email
%p
  %b Role:
  = @user.role

- if @can_edit
  = link_to 'Edit', edit_user_path(@user), :class => 'btn btn-edit'
= link_to 'Back', users_path, :class => 'btn'
