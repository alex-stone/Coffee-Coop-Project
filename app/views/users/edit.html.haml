%h1 Editing user

= render 'form'

= link_to 'Show', @user, :class => 'btn'
= link_to 'Back', users_path, :class => 'btn'
- if @is_admin
  = link_to 'Destroy', transaction_path, :confirm => 'Are you sure?', :method => 'delete', :class => 'btn btn-delete'
